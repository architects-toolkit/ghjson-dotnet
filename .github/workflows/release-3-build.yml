name: Release 3 - Build and Attach Release Assets

# Description: Builds the NuGet packages for a published GitHub Release and
# attaches the .nupkg/.snupkg artifacts to it. The release (and its bare X.Y.Z
# tag) is created earlier by release-2-tag-on-merge.yml; publishing the draft
# triggers this workflow.
#
# Triggers:
# - release.types: published
#
# Permissions:
# - contents:write - Required to attach assets to the release

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '7.0.x'

      - name: Get version from tag
        id: version
        shell: pwsh
        run: |
          $tag = "${{ github.event.release.tag_name }}"
          $version = $tag -replace '^v', ''
          echo "version=$version" >> $env:GITHUB_OUTPUT

      - name: Update version in Directory.Build.props
        shell: pwsh
        run: |
          $propsPath = "Directory.Build.props"
          $content = Get-Content $propsPath -Raw
          $content = $content -replace '<Version>.*?</Version>', "<Version>${{ steps.version.outputs.version }}</Version>"
          Set-Content $propsPath $content

      - name: Restore dependencies
        run: dotnet restore

      - name: Build Release
        run: dotnet build --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build --verbosity normal

      - name: Pack NuGet packages
        run: dotnet pack --configuration Release --no-build --output ./nupkgs

      - name: Upload NuGet packages as artifacts
        uses: actions/upload-artifact@v6
        with:
          name: nuget-packages
          path: ./nupkgs/*.nupkg

      - name: Attach NuGet packages to release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            ./nupkgs/*.nupkg
            ./nupkgs/*.snupkg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
